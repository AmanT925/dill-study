#############################################
# Guide Grok Example Environment Variables  #
#############################################
# Copy this file to .env.local and fill in values. Do NOT commit .env.local.

# --- Core Firebase (REQUIRED) ---
VITE_FIREBASE_API_KEY=""
VITE_FIREBASE_AUTH_DOMAIN=""
VITE_FIREBASE_PROJECT_ID=""
VITE_FIREBASE_STORAGE_BUCKET=""
VITE_FIREBASE_MESSAGING_SENDER_ID=""
VITE_FIREBASE_APP_ID=""

# --- AI (Optional) ---
VITE_GEMINI_API_KEY=""
VITE_GEMINI_MODEL=gemini-2.5-flash
VITE_GEMINI_GUIDANCE_MODEL=gemini-pro
# OpenAI key only if OpenAI integration is enabled
VITE_OPENAI_API_KEY=""

# --- ElevenLabs (Optional TTS) ---
VITE_ELEVENLABS_API_KEY=""
VITE_ELEVENLABS_VOICE_ID=""

# --- Reminder / Script Testing Overrides (usually set inline, optional) ---
TEST_MINUTES_MODE=true
TEST_WINDOWS=1,2
TEST_WINDOW_MINUTES=1
TEST_DRY_RUN=true

# Notes:
# 1. Keys prefixed with VITE_ are bundled client-side – treat them as semi-public.
# 2. Rotate any leaked keys immediately.
# 3. For server-only secrets use a backend / serverless function layer (not included yet).


